extends ./layout.jade

block title
	title Stenography - dctWatermark

block content
	.jumbotron
		h2 DCT based watermarking
		ul
			li Encode low frequency signal of watermark image into high frequency portion of color image
			li <strong>Slice:</strong> The slice of image used to store watermark
			li <strong>Shift:</strong> Shift factor before encode into color image
			li <strong>Remember:</strong> You must put right parameters to get the image back
			li <Strong>Note:</strong> If the image size dis-match, the watermark image will be stretch to match color image

	.row
		.col-sm-6
			.thumbnail#color.dropable
				img.pointer(src='/img/color.png' alt='color image')
				.caption
					input.center-block(type='file' name='color')
					h3.text-center Click to upload your color image
		.col-sm-6
			.thumbnail#watermark.dropable
				img.pointer(src='/img/watermark.png', alt='watermark image')
				.caption
					input.center-block(type='file' name='watermark')
					h3.text-center Click to upload your watermark image
	.row
		.col-sm-12
			.thumbnail.text-center
				p
					label Slice:
					span.sliderValue#sliceValue 2
					input#slice(data-slider-min='1' data-slider-max='8' data-slider-step='1' data-slider-value='2')
				p
					label Shift:
					span.sliderValue#shiftValue 4
					input#shift(data-slider-min='1' data-slider-max='16' data-slider-step='1' data-slider-value='4')

				button.btn.btn-lg.btn-primary#start(type='button') Start DCT Watermarking

	.row#result
		.col-sm-12
			.thumbnail.text-center
				img(src='', alt='watermarked image')
				.caption
					button.btn.btn-lg.btn-primary(type='button') Download Watermarked image
